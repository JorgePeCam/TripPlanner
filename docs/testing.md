# Testing

## TripPlannerKit (SwiftPM)
Run domain/data tests with:
cd Packages/TripPlannerKit
swift test -v

Note: Xcode may not list SwiftPM test targets reliably for local packages. Terminal execution is the source of truth.

## TripPlannerApp (Xcode)
Run app unit/UI tests via Xcode scheme (⌘U).
